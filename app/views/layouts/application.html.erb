<!DOCTYPE html>
<html>
  <head>
    <title>Freshreader</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <meta name="viewport" content="width=device-width, initial-scale=1">

		<link href="https://fonts.googleapis.com/css?family=Inter:400,700|Roboto+Mono&display=swap" rel="stylesheet">
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
    <%= favicon_link_tag %>
  </head>

  <body>
    <div id="main-container">
      <div class="nav">
        <div>
          <div class="flex">
            <div class="flex-1">
              <a class="internal-link no-border-bottom" href="<% if logged_in? %>/articles<% else %>/<% end %>">
                <img id="nav-logo" src="<%= image_url("logo.svg") %>"/>
              </a>
            </div>
            <p>
              <% if logged_in? %>
                <%= link_to "View list", '/articles', class: "internal-link nav-link" %>
                <%= link_to "View account", '/account', class: "internal-link nav-link" %>
                <%= link_to "Log out", '/logout', method: :post, class: "internal-link nav-link" %>
              <% else %>
                <%= link_to "Log in", '/login', method: :get, class: "internal-link nav-link" %>
              <% end %>
            </p>
          </div>
        </div>
      </div>

      <div class="wrapper" id="flashes-wrapper">
        <% flash.each do |name, msg| %>
          <div class="flash flash_<%= name %>">
            <%= msg %>
          </div>
        <% end %>
      </div>

      <div>
        <%= yield %>
      </div>

      <footer>
        <div class="wrapper">
          <p>
            Built by <a target="_blank" href="https://twitter.com/vaillancourtmax">Maxime Vaillancourt</a> in Montreal, Canada ✌️
          </p>
          <div>
            <%= link_to "🍃 A few words on privacy", '/privacy', class: "internal-link" %>
            &middot;
            <%= link_to "👨‍💻 Open source", 'https://github.com/freshreader' %>
            &middot;
            <%= link_to "👋 Feedback", 'https://github.com/freshreader/core/issues' %>
          </div>
        </div>
      </footer>
    </div>
  </body>
</html>
